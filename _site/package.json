{
  "scripts": {
    "sass-dev": "sass --no-source-map --watch --update --style=expanded assets/scss:assets/css",
    "sass-prod": "sass --no-source-map --style=compressed assets/scss:assets/css",
    "pdf": "prince _site/index.html -o resume.pdf"
  },
  "dependencies": {
    "sass": "^1.72.0"
  }
}
